#ifndef GRADIENTS_METAL_PROBE_H
#define GRADIENTS_METAL_PROBE_H

/* Placeholder used to validate the Objective-C build path before the real
 * Metal backend lands. Not registered anywhere; carries no behavior. */
int _gd_metal_toolchain_probe(void);

#endif /* GRADIENTS_METAL_PROBE_H */
